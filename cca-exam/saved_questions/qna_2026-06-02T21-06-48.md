# CCA-F Mock Exam — Questions & Solutions
_Generated: 02 Jun 2026, 09:10 PM · Model: claude-sonnet-4-20250514_

---

## Part 1 — Questions

### Q1. [Domain 1 — Agentic Architecture & Orchestration]
**Scenario:** Multi-Agent Pipeline

You're designing a multi-agent pipeline for content moderation where Agent A classifies content type, Agent B performs safety analysis, and Agent C generates moderation decisions. How should you architect the orchestration layer to handle agent failures and maintain system reliability?

  - **A.** Implement a centralized orchestrator with circuit breakers for each agent, exponential backoff on retries, and fallback workflows that can complete moderation with reduced agent participation.
  - **B.** Chain the agents sequentially with direct API calls between them, implementing retry logic in each agent to handle downstream failures and maintaining shared state in a database.
  - **C.** Use a pub-sub messaging system where each agent subscribes to the previous agent's output topic, with dead letter queues for failed messages and manual intervention processes.
  - **D.** Deploy agents as microservices with load balancers and let each agent poll a shared work queue, implementing timeouts and basic retry mechanisms without coordination.

---

### Q2. [Domain 2 — Tool Design & MCP Integration]
**Scenario:** Enterprise Knowledge Base

You're building an enterprise knowledge base system where Claude needs to access multiple data sources including SharePoint, Confluence, and internal databases. What's the optimal MCP tool design strategy for this multi-source search capability?

  - **A.** Create separate MCP tools for each data source with consistent return schemas, implementing query routing logic in Claude prompts to determine which sources to search based on user intent.
  - **B.** Build a single unified MCP tool that accepts a raw search query object containing source preferences, query text, and filter parameters, then handles all routing and aggregation internally.
  - **C.** Design source-specific MCP tools with different parameter schemas optimized for each system, and create a meta-tool that orchestrates multi-source searches through parallel tool calls.
  - **D.** Implement individual MCP tools for each source with standardized parameters, then use Claude's native tool calling to parallelize searches and synthesize results from multiple sources.

---

### Q3. [Domain 3 — Claude Code Configuration & Workflows]
**Scenario:** Code Review CI/CD

You're configuring Claude to perform automated code reviews in a CI/CD pipeline where it analyzes pull requests for security vulnerabilities, code quality, and adherence to team standards. How should you structure the Claude workflow configuration to ensure consistent, reliable reviews?

  - **A.** Configure a single comprehensive prompt that includes all review criteria, code context, and examples, with the entire pull request diff passed as one large context block for thorough analysis.
  - **B.** Design a multi-stage workflow with separate Claude instances for security, quality, and standards review, each with specialized prompts and focused context, then aggregate results through a final synthesis step.
  - **C.** Create a streaming workflow that processes files incrementally, maintaining conversation history across files to build context, and generates a cumulative review document as it progresses through the changeset.
  - **D.** Set up a template-based system with dynamic prompt generation based on file types and change patterns, using minimal context per request and relying on Claude's general knowledge for most review criteria.

---

### Q4. [Domain 1 — Agentic Architecture & Orchestration]
**Scenario:** Multi-Agent Pipeline

You're designing a multi-agent content moderation pipeline where Claude agents specialize in text analysis, image context understanding, and policy enforcement. How should you architect the orchestration layer to ensure reliable agent coordination and graceful degradation?

  - **A.** Implement a centralized orchestrator with circuit breakers for each agent, using async message queues with dead letter handling and exponential backoff for failed agent communications.
  - **B.** Create a peer-to-peer mesh where agents directly communicate through shared database state, with each agent polling for work items and updating status flags in real-time.
  - **C.** Use a synchronous chain where each agent waits for the previous one to complete, implementing simple retry logic with fixed delays and direct HTTP calls between agents.
  - **D.** Deploy agents as independent microservices that communicate through event streaming, with each agent subscribing to all events and filtering based on internal business logic.

---

### Q5. [Domain 4 — Prompt Engineering & Structured Output]
**Scenario:** Enterprise Knowledge Base

Your enterprise knowledge base system needs Claude to extract structured metadata from unstructured documents and ensure consistent schema compliance across thousands of document types. What prompt engineering approach best balances accuracy and reliability?

  - **A.** Use dynamic few-shot examples selected based on document similarity scores, with schema validation performed entirely within Claude's reasoning process and fallback to generic extraction patterns.
  - **B.** Implement a two-stage approach with schema-constrained JSON output using Claude's structured generation, followed by separate validation logic that enforces business rules and data consistency.
  - **C.** Create document-type-specific prompts with hardcoded examples for each category, using Claude to generate free-form descriptions that are parsed by downstream rule-based systems.
  - **D.** Design a single comprehensive prompt with all possible schema fields and examples, allowing Claude to dynamically choose the appropriate extraction strategy based on document content analysis.

---

### Q6. [Domain 5 — Context Management & Reliability]
**Scenario:** Developer Productivity Tool

You're building a developer productivity tool where Claude analyzes large codebases to suggest refactoring opportunities. The system needs to maintain context across multiple analysis sessions while handling repositories with millions of lines of code. How should you design the context management strategy?

  - **A.** Implement hierarchical context chunking with smart retrieval, maintaining session state in external storage and dynamically loading relevant code segments based on analysis focus areas.
  - **B.** Load the entire codebase into Claude's context window using aggressive compression techniques, maintaining a single long-running conversation thread for all analysis requests.
  - **C.** Create separate Claude instances for each code module with no shared context, aggregating results through a coordination service that merges individual analysis outputs.
  - **D.** Use sliding window context management where each analysis request includes the previous N lines of code, with overlap strategies to maintain continuity across analysis boundaries.

---

### Q7. [Domain 1 — Agentic Architecture & Orchestration]
**Scenario:** Multi-Agent Pipeline

You're designing a multi-agent pipeline for enterprise document processing where Claude agents handle extraction, validation, and routing tasks. Each agent needs to coordinate with others while maintaining clear boundaries and error recovery. What is the most appropriate orchestration pattern?

  - **A.** Implement a centralized coordinator that manages agent state transitions through explicit handoffs, with each agent publishing completion events to a message queue for the next agent to consume.
  - **B.** Create a shared database where all agents continuously poll for work items, using database locks to prevent conflicts and allowing any agent to pick up any task type based on availability.
  - **C.** Chain agents directly together with synchronous API calls, where each agent immediately invokes the next agent in sequence and waits for the complete pipeline to finish before returning results.
  - **D.** Use a publish-subscribe pattern where agents broadcast their outputs to all other agents, letting each agent decide whether to process the message based on its internal logic and current workload.

---

### Q8. [Domain 2 — Tool Design & MCP Integration]
**Scenario:** Customer Support Agent

You're building a customer support agent that needs to access customer order history, update ticket status, and escalate to human agents. The system handles thousands of concurrent conversations with varying data access patterns. How should you design the tool integration architecture?

  - **A.** Create individual MCP tools for each database table operation, allowing Claude to compose complex queries by calling multiple granular tools in sequence based on the conversation context.
  - **B.** Build composite MCP tools that combine related operations (like get_customer_profile including orders, tickets, and preferences) with intelligent caching and connection pooling for high-throughput scenarios.
  - **C.** Implement a single universal database_query tool that accepts raw SQL parameters, giving Claude maximum flexibility to construct any query needed for customer support scenarios.
  - **D.** Design stateful MCP tools that maintain conversation context between calls, automatically remembering customer information and previous queries to reduce redundant data fetching.

---

### Q9. [Domain 3 — Claude Code Configuration & Workflows]
**Scenario:** Code Review CI/CD

You're implementing a Claude-powered code review system that analyzes pull requests, suggests improvements, and generates security reports. The system needs to handle varying code sizes and maintain consistent quality across different programming languages. What workflow configuration approach ensures reliable operation?

  - **A.** Configure dynamic context windows that automatically expand based on file size, using streaming responses to handle large codebases while maintaining full file context for accurate analysis.
  - **B.** Implement a chunking strategy with overlapping context windows and structured prompts that maintain code relationship awareness, combined with result synthesis for comprehensive reviews.
  - **C.** Set up language-specific prompt templates with fixed context limits, preprocessing code to remove comments and whitespace to maximize the amount of actual code that fits in each request.
  - **D.** Use a single comprehensive prompt that includes all files simultaneously, relying on Claude's context management to prioritize the most important sections automatically during analysis.

---

### Q10. [Domain 4 — Prompt Engineering & Structured Output]
**Scenario:** Data Extraction

You're building a data extraction system that processes various document formats (PDFs, emails, contracts) and needs to return structured data in different schemas depending on document type. How should you architect the prompt engineering and output validation?

  - **A.** Create a single mega-prompt that handles all document types with conditional logic, using a union schema that includes all possible fields across document types.
  - **B.** Design document-type-specific prompts with tailored schemas, use structured output validation with strict typing, and implement schema versioning for backwards compatibility.
  - **C.** Use a two-stage approach where the first prompt classifies document type and extracts raw text, then pass everything to a second prompt that formats the output.
  - **D.** Implement dynamic prompt generation that builds schemas on-the-fly based on document content analysis, with flexible validation that adapts to detected patterns.

---

### Q11. [Domain 5 — Context Management & Reliability]
**Scenario:** Enterprise Knowledge Base

Your enterprise knowledge base system serves multiple departments with varying context windows (legal docs, technical specs, HR policies). Users often ask complex questions requiring synthesis across multiple documents. How do you manage context and ensure reliable responses?

  - **A.** Implement context chunking with semantic search to retrieve relevant passages, use context window monitoring to prevent saturation, and add confidence scoring with graceful degradation for incomplete information.
  - **B.** Load all relevant documents into context for each query to ensure completeness, implement retry logic for context overflow errors, and cache full document sets per department.
  - **C.** Use a sliding context window that automatically drops older information when limits are approached, prioritizing recent documents and user query context over historical information.
  - **D.** Implement parallel context processing where multiple Claude instances handle different document sets, then merge results using a coordinator prompt that synthesizes all outputs.

---

### Q12. [Domain 1 — Agentic Architecture & Orchestration]
**Scenario:** Multi-Agent Pipeline

You're designing a content moderation pipeline where multiple AI agents handle different aspects: toxicity detection, spam classification, and policy compliance checking. Each agent needs to make decisions that affect downstream processing. How should you orchestrate the agent interactions?

  - **A.** Implement a sequential pipeline where each agent passes results to the next, with state management handled through a central coordinator that tracks decisions and manages rollbacks.
  - **B.** Use a hub-and-spoke model where all agents process content simultaneously and send results to a decision aggregator that applies weighted scoring and conflict resolution rules.
  - **C.** Create a hierarchical system where a supervisor agent delegates tasks to specialist agents, collects their outputs, and makes final moderation decisions with full context visibility.
  - **D.** Design independent agents that each make autonomous decisions and write to a shared database, with downstream conflict resolution handled by business logic outside the AI system.

---

### Q13. [Domain 2 — Tool Design & MCP Integration]
**Scenario:** Developer Productivity Tool

You're building a developer productivity tool that integrates with multiple IDEs and version control systems. The tool needs to provide Claude with real-time access to code repositories, documentation, and project configurations. Which MCP integration approach best supports this multi-tool environment?

  - **A.** Create a single monolithic MCP server that handles all IDE integrations, Git operations, and documentation access through one unified interface with shared connection pooling.
  - **B.** Implement separate MCP servers for each tool type (IDE server, Git server, docs server) with Claude managing connections and orchestrating cross-server operations through a coordinator pattern.
  - **C.** Build MCP servers as lightweight, stateless adapters for each external system, allowing Claude to compose operations across multiple servers while maintaining clear separation of concerns.
  - **D.** Design a hierarchical MCP architecture where a master server proxies requests to specialized sub-servers, caching responses and maintaining session state across all integrations.

---

### Q14. [Domain 3 — Claude Code Configuration & Workflows]
**Scenario:** Content Moderation

Your content moderation system processes user-generated content from multiple social platforms and needs to handle varying content types, languages, and moderation policies. You're implementing a Claude-based workflow that must maintain consistent decisions while adapting to platform-specific rules. How should you structure the Claude configuration?

  - **A.** Use a single Claude instance with dynamic system messages that inject platform-specific rules and content type handlers based on incoming request metadata.
  - **B.** Deploy separate Claude instances for each platform-content type combination, with pre-configured system messages and validation rules specific to each use case.
  - **C.** Create a master Claude coordinator that routes content to specialized Claude workers, each configured for specific moderation domains with standardized output schemas.
  - **D.** Implement a shared Claude instance pool with runtime configuration injection, where moderation rules and content handlers are loaded from a database per request.

---

### Q15. [Domain 4 — Prompt Engineering & Structured Output]
**Scenario:** Enterprise Knowledge Base

You're developing an enterprise knowledge base system where Claude must extract structured information from diverse document types (contracts, technical specs, meeting notes) and generate consistent metadata for search indexing. The system needs to handle documents with varying schemas while maintaining data quality. What prompt engineering approach best ensures reliable structured output?

  - **A.** Use a single comprehensive prompt with conditional logic that detects document type and applies appropriate extraction rules, outputting a flexible JSON schema that adapts to content.
  - **B.** Implement document type classification followed by specialized extraction prompts, each with strict JSON schemas and validation rules tailored to specific document categories.
  - **C.** Create a multi-step pipeline where Claude first analyzes document structure, then applies generic extraction templates with dynamic field mapping based on detected patterns.
  - **D.** Design a universal extraction prompt with extensive examples covering all document types, using a large unified schema that includes optional fields for every possible data point.

---

### Q16. [Domain 1 — Agentic Architecture & Orchestration]
**Scenario:** Customer Support Agent

You're designing a customer support agent that needs to handle complex multi-turn conversations while maintaining context across sessions and escalating to human agents when needed. What is the most appropriate agentic architecture approach?

  - **A.** Implement a single Claude instance with conversation memory stored in system prompts, using a simple rule-based escalation trigger when confidence scores drop below a threshold.
  - **B.** Deploy a multi-agent system with specialized agents for different support domains, a central orchestrator managing conversation state, and circuit breakers preventing infinite loops between agents.
  - **C.** Create a monolithic agent that stores all conversation history in the context window and uses function calling to directly modify the customer database without validation layers.
  - **D.** Design a pipeline where each customer message triggers a new Claude instance with the full conversation history, using synchronous calls to multiple external APIs for real-time data enrichment.

---

### Q17. [Domain 2 — Tool Design & MCP Integration]
**Scenario:** E-commerce Platform

Your e-commerce platform needs Claude to process customer orders, update inventory, and send notifications through various channels. How should you design the tool integration using MCP?

  - **A.** Create separate MCP servers for inventory management, order processing, and notifications, each with idempotent operations and proper error handling, connected through a unified tool registry.
  - **B.** Build a single MCP server that combines all e-commerce operations into one tool with a large JSON parameter containing all possible order and inventory data fields.
  - **C.** Implement direct API calls from Claude to each service without MCP abstraction, using retry logic with exponential backoff for all operations regardless of their nature.
  - **D.** Design MCP tools that accept raw database queries as parameters, allowing Claude to directly construct SQL statements for maximum flexibility in data operations.

---

### Q18. [Domain 3 — Claude Code Configuration & Workflows]
**Scenario:** Code Review CI/CD

You're implementing Claude in a CI/CD pipeline to perform automated code reviews for pull requests in a large enterprise development environment. What configuration approach ensures reliable and secure operation?

  - **A.** Configure Claude with a broad system prompt containing all coding standards, use a single workflow that processes entire codebases at once, and implement unlimited retries for failed reviews.
  - **B.** Set up modular workflows with specific prompts for different code review aspects, implement proper context windowing for large files, and use circuit breakers to handle API failures gracefully.
  - **C.** Create a workflow that concatenates all changed files into one massive prompt, stores sensitive configuration data in the system message, and uses synchronous processing for real-time feedback.
  - **D.** Design workflows that inject raw git diff outputs without sanitization, use dynamic prompt construction from user input, and implement aggressive caching that persists across different repositories.

---

### Q19. [Domain 4 — Prompt Engineering & Structured Output]
**Scenario:** Data Extraction

You're building a data extraction system that processes financial documents and outputs structured JSON. The extracted data must conform to a strict schema for regulatory compliance, but document formats vary significantly across different financial institutions.

  - **A.** Use function calling with a comprehensive schema definition that includes all possible fields, and implement fallback logic to handle missing or malformed data gracefully.
  - **B.** Chain multiple specialized prompts where each extracts a specific data category, then merge results using a final validation prompt that ensures schema compliance.
  - **C.** Implement a single comprehensive prompt with detailed examples and use regex post-processing to enforce the JSON schema structure regardless of Claude's output format.
  - **D.** Use system prompts to define the schema once, then rely on few-shot examples in user messages to guide extraction behavior for each document type.

---

### Q20. [Domain 5 — Context Management & Reliability]
**Scenario:** Enterprise Knowledge Base

Your enterprise knowledge base system needs to handle conversations that can span multiple topics and reference various internal documents. Users often ask follow-up questions that require context from earlier in the conversation, but the system sometimes fails when conversations become very long.

  - **A.** Implement sliding window context management that preserves the most recent exchanges while maintaining a summary of earlier conversation topics and key decisions.
  - **B.** Store the entire conversation history in a vector database and retrieve relevant segments based on semantic similarity to the current question.
  - **C.** Use a fixed context window and simply truncate older messages when the limit is reached, ensuring the system always has the most recent context available.
  - **D.** Implement conversation branching where each new topic starts a fresh context, but maintain cross-references to related earlier discussions through metadata.

---

### Q21. [Domain 1 — Agentic Architecture & Orchestration]
**Scenario:** Multi-Agent Pipeline

You're designing a content moderation pipeline where multiple AI agents analyze user-generated content for different violation types (hate speech, spam, inappropriate images). Each agent needs to make independent assessments, but the final decision requires considering all agent outputs with different confidence thresholds.

  - **A.** Implement a coordinator agent that receives all individual agent assessments and makes final moderation decisions based on weighted confidence scores and business rules.
  - **B.** Create a sequential pipeline where each agent processes content in order, with early termination if any agent detects a clear violation above the threshold.
  - **C.** Use a voting mechanism where agents process content simultaneously and the majority decision determines the outcome, with ties resolved by the highest confidence score.
  - **D.** Design agents to communicate directly with each other during processing, sharing intermediate findings to improve overall accuracy before reaching consensus.

---

### Q22. [Domain 2 — Tool Design & MCP Integration]
**Scenario:** Customer Support Agent

You're building a customer support agent that needs to access order history, update ticket status, and send notifications. Which tool design approach provides the best balance of safety and functionality?

  - **A.** Create separate tools for read operations (get_order_history) and write operations (update_ticket, send_notification) with explicit confirmation steps for state changes.
  - **B.** Build a single unified tool called customer_service_handler that accepts a raw JSON object parameter containing all customer data and operation types to minimize tool switching overhead.
  - **C.** Design tools that automatically retry failed operations indefinitely with exponential backoff to ensure eventual consistency across all customer service systems.
  - **D.** Implement tools that directly expose database cursors and connection objects to Claude for maximum flexibility in handling complex customer queries.

---

### Q23. [Domain 4 — Prompt Engineering & Structured Output]
**Scenario:** E-commerce Platform

Your e-commerce platform needs Claude to generate product recommendations with specific pricing tiers and inventory constraints. How should you structure the prompt and output to ensure consistent, actionable results?

  - **A.** Use a conversational prompt asking Claude to 'recommend some products' and parse the natural language response with regex patterns to extract product IDs and prices.
  - **B.** Include all product catalog data directly in the system prompt and ask Claude to select items, then manually validate the output against current inventory levels.
  - **C.** Design a structured prompt with clear constraints (budget range, category filters) and specify a JSON schema requiring product_id, price, and availability_status fields for each recommendation.
  - **D.** Implement a multi-turn conversation where Claude asks follow-up questions about user preferences before providing unstructured recommendations in markdown format.

---

### Q24. [Domain 3 — Claude Code Configuration & Workflows]
**Scenario:** Code Review CI/CD

You're implementing Claude in a CI/CD pipeline to review pull requests for security issues and code quality. Which configuration approach ensures reliable operation under varying workloads?

  - **A.** Configure the system to process all pull request files simultaneously in a single Claude call to minimize API overhead and maintain consistent review standards across the codebase.
  - **B.** Set up file-by-file processing with circuit breakers, timeout controls, and graceful degradation that skips non-critical files when the system is under heavy load.
  - **C.** Implement a queue-based system that accumulates all pending reviews and processes them in large batches every hour to optimize for throughput over latency.
  - **D.** Configure automatic retry logic that continues attempting to process failed reviews indefinitely until successful, ensuring no security issues are missed.

---

### Q25. [Domain 1 — Agentic Architecture & Orchestration]
**Scenario:** Multi-Agent Pipeline

You're designing a multi-agent system where Claude agents handle document processing, data validation, and report generation in sequence. How should you architect the inter-agent communication and state management?

  - **A.** Implement a centralized orchestrator with explicit state transitions and structured message passing between agents, using JSON schemas for validation.
  - **B.** Allow agents to directly call each other's APIs with shared memory access, enabling real-time collaboration and immediate state synchronization.
  - **C.** Use a pub/sub messaging system where agents broadcast all their outputs to all other agents, letting each agent filter relevant information.
  - **D.** Chain agents together with direct function calls, passing the full conversation history and accumulated context through each agent in the pipeline.

---

## Part 2 — Solutions

### Q1. You're designing a multi-agent pipeline for content moderation where Agent …

**✅ Correct Answer: A**
> Implement a centralized orchestrator with circuit breakers for each agent, exponential backoff on retries, and fallback workflows that can complete moderation with reduced agent participation.

The centralized orchestrator approach provides proper failure isolation through circuit breakers, prevents cascading failures with exponential backoff, and maintains system availability through fallback workflows. This architecture ensures the pipeline can gracefully degrade while maintaining essential moderation capabilities.

**⚠️ Anti-Pattern: B**
> Chain the agents sequentially with direct API calls between them, implementing retry logic in each agent to handle downstream failures and maintaining shared state in a database.

Direct chaining creates tight coupling and cascading failure risks where one agent failure brings down the entire pipeline.

---

### Q2. You're building an enterprise knowledge base system where Claude needs to a…

**✅ Correct Answer: D**
> Implement individual MCP tools for each source with standardized parameters, then use Claude's native tool calling to parallelize searches and synthesize results from multiple sources.

Individual standardized MCP tools with parallel calling leverages Claude's native tool orchestration capabilities while maintaining clear separation of concerns and enabling optimal concurrent execution. This approach provides both modularity and performance without overcomplicating the tool interface.

**⚠️ Anti-Pattern: B**
> Build a single unified MCP tool that accepts a raw search query object containing source preferences, query text, and filter parameters, then handles all routing and aggregation internally.

Using a raw object parameter creates a non-idempotent tool that's difficult for Claude to reason about and increases prompt injection surface area.

---

### Q3. You're configuring Claude to perform automated code reviews in a CI/CD pipe…

**✅ Correct Answer: B**
> Design a multi-stage workflow with separate Claude instances for security, quality, and standards review, each with specialized prompts and focused context, then aggregate results through a final synthesis step.

The multi-stage workflow with specialized Claude instances provides focused analysis while preventing context saturation, enables parallel processing for faster reviews, and allows fine-tuning of each review aspect independently. The synthesis step ensures coherent final output while maintaining the benefits of specialized review stages.

**⚠️ Anti-Pattern: A**
> Configure a single comprehensive prompt that includes all review criteria, code context, and examples, with the entire pull request diff passed as one large context block for thorough analysis.

Passing entire pull request diffs as one large context block leads to context saturation and reduces Claude's ability to focus on specific review criteria.

---

### Q4. You're designing a multi-agent content moderation pipeline where Claude age…

**✅ Correct Answer: A**
> Implement a centralized orchestrator with circuit breakers for each agent, using async message queues with dead letter handling and exponential backoff for failed agent communications.

A centralized orchestrator with circuit breakers prevents cascading failures and provides controlled degradation, while async queues with dead letter handling ensure reliable message delivery and proper error recovery in multi-agent systems.

**⚠️ Anti-Pattern: C**
> Use a synchronous chain where each agent waits for the previous one to complete, implementing simple retry logic with fixed delays and direct HTTP calls between agents.

Synchronous chaining creates brittle dependencies where one agent failure blocks the entire pipeline, and fixed delay retries can cause unbounded retry storms.

---

### Q5. Your enterprise knowledge base system needs Claude to extract structured me…

**✅ Correct Answer: B**
> Implement a two-stage approach with schema-constrained JSON output using Claude's structured generation, followed by separate validation logic that enforces business rules and data consistency.

The two-stage approach leverages Claude's structured generation capabilities for consistent output format while maintaining separation of concerns between extraction and business rule validation. This architecture ensures schema compliance and allows independent evolution of validation logic.

**⚠️ Anti-Pattern: D**
> Design a single comprehensive prompt with all possible schema fields and examples, allowing Claude to dynamically choose the appropriate extraction strategy based on document content analysis.

A comprehensive prompt with all schema fields leads to context bloat and reduces Claude's ability to focus on relevant extraction patterns for specific document types.

---

### Q6. You're building a developer productivity tool where Claude analyzes large c…

**✅ Correct Answer: A**
> Implement hierarchical context chunking with smart retrieval, maintaining session state in external storage and dynamically loading relevant code segments based on analysis focus areas.

Hierarchical context chunking with smart retrieval prevents context saturation while maintaining relevant contextual information through external session state. This approach scales to large codebases by loading only necessary code segments based on current analysis needs.

**⚠️ Anti-Pattern: B**
> Load the entire codebase into Claude's context window using aggressive compression techniques, maintaining a single long-running conversation thread for all analysis requests.

Loading entire codebases into context causes context saturation and will exceed Claude's context limits, making the system unreliable for large repositories.

---

### Q7. You're designing a multi-agent pipeline for enterprise document processing …

**✅ Correct Answer: A**
> Implement a centralized coordinator that manages agent state transitions through explicit handoffs, with each agent publishing completion events to a message queue for the next agent to consume.

A centralized coordinator with explicit handoffs and event-driven communication provides clear orchestration boundaries while enabling proper error recovery and state management. This pattern maintains agent autonomy while ensuring coordinated execution flow.

**⚠️ Anti-Pattern: C**
> Chain agents directly together with synchronous API calls, where each agent immediately invokes the next agent in sequence and waits for the complete pipeline to finish before returning results.

Synchronous chaining creates tight coupling and eliminates the ability to implement circuit breakers or handle partial failures gracefully.

---

### Q8. You're building a customer support agent that needs to access customer orde…

**✅ Correct Answer: B**
> Build composite MCP tools that combine related operations (like get_customer_profile including orders, tickets, and preferences) with intelligent caching and connection pooling for high-throughput scenarios.

Composite MCP tools with intelligent caching provide the right abstraction level for customer support scenarios, reducing API calls while maintaining semantic clarity. Connection pooling ensures the system can handle high concurrency without resource exhaustion.

**⚠️ Anti-Pattern: C**
> Implement a single universal database_query tool that accepts raw SQL parameters, giving Claude maximum flexibility to construct any query needed for customer support scenarios.

A raw SQL parameter tool creates a massive prompt injection surface and violates the principle of providing semantic, bounded operations to Claude.

---

### Q9. You're implementing a Claude-powered code review system that analyzes pull …

**✅ Correct Answer: B**
> Implement a chunking strategy with overlapping context windows and structured prompts that maintain code relationship awareness, combined with result synthesis for comprehensive reviews.

A chunking strategy with overlapping windows and relationship awareness prevents context saturation while ensuring comprehensive analysis. Result synthesis allows the system to provide cohesive feedback across large codebases without losing important cross-file dependencies.

**⚠️ Anti-Pattern: D**
> Use a single comprehensive prompt that includes all files simultaneously, relying on Claude's context management to prioritize the most important sections automatically during analysis.

Including all files simultaneously leads to context bloat and reduces Claude's ability to provide detailed analysis of individual components.

---

### Q10. You're building a data extraction system that processes various document fo…

**✅ Correct Answer: B**
> Design document-type-specific prompts with tailored schemas, use structured output validation with strict typing, and implement schema versioning for backwards compatibility.

Document-type-specific prompts with tailored schemas provide the best balance of accuracy and maintainability, while structured output validation ensures data quality and schema versioning enables system evolution.

**⚠️ Anti-Pattern: A**
> Create a single mega-prompt that handles all document types with conditional logic, using a union schema that includes all possible fields across document types.

A single mega-prompt leads to context bloat and reduces extraction accuracy across different document types.

---

### Q11. Your enterprise knowledge base system serves multiple departments with vary…

**✅ Correct Answer: A**
> Implement context chunking with semantic search to retrieve relevant passages, use context window monitoring to prevent saturation, and add confidence scoring with graceful degradation for incomplete information.

Context chunking with semantic search optimizes context usage while monitoring prevents saturation, and confidence scoring enables reliable operation when information is incomplete.

**⚠️ Anti-Pattern: B**
> Load all relevant documents into context for each query to ensure completeness, implement retry logic for context overflow errors, and cache full document sets per department.

Loading all documents leads to immediate context saturation and makes the system unreliable for complex queries.

---

### Q12. You're designing a content moderation pipeline where multiple AI agents han…

**✅ Correct Answer: C**
> Create a hierarchical system where a supervisor agent delegates tasks to specialist agents, collects their outputs, and makes final moderation decisions with full context visibility.

A hierarchical supervisor pattern provides clear orchestration control and context visibility while enabling specialized agent expertise and centralized decision-making authority.

**⚠️ Anti-Pattern: D**
> Design independent agents that each make autonomous decisions and write to a shared database, with downstream conflict resolution handled by business logic outside the AI system.

Independent agents writing to shared state creates non-idempotent operations and race conditions without proper coordination mechanisms.

---

### Q13. You're building a developer productivity tool that integrates with multiple…

**✅ Correct Answer: C**
> Build MCP servers as lightweight, stateless adapters for each external system, allowing Claude to compose operations across multiple servers while maintaining clear separation of concerns.

Lightweight, stateless MCP servers with clear separation of concerns provide the best modularity and reliability for multi-tool environments. This approach allows independent scaling, easier maintenance, and reduces the blast radius when individual integrations fail.

**⚠️ Anti-Pattern: A**
> Create a single monolithic MCP server that handles all IDE integrations, Git operations, and documentation access through one unified interface with shared connection pooling.

A monolithic MCP server creates tight coupling and becomes a single point of failure across all developer tools.

---

### Q14. Your content moderation system processes user-generated content from multip…

**✅ Correct Answer: C**
> Create a master Claude coordinator that routes content to specialized Claude workers, each configured for specific moderation domains with standardized output schemas.

A master coordinator with specialized workers provides the optimal balance of consistency and specialization, allowing domain-specific expertise while maintaining centralized orchestration. This architecture supports complex routing logic and independent scaling of different moderation types.

**⚠️ Anti-Pattern: D**
> Implement a shared Claude instance pool with runtime configuration injection, where moderation rules and content handlers are loaded from a database per request.

Runtime configuration injection from databases creates context bloat and introduces potential prompt injection surfaces through dynamic rule loading.

---

### Q15. You're developing an enterprise knowledge base system where Claude must ext…

**✅ Correct Answer: B**
> Implement document type classification followed by specialized extraction prompts, each with strict JSON schemas and validation rules tailored to specific document categories.

Document type classification followed by specialized extraction ensures the highest data quality and consistency by using domain-specific schemas and validation rules. This approach minimizes hallucination and provides predictable output structures for downstream processing.

**⚠️ Anti-Pattern: A**
> Use a single comprehensive prompt with conditional logic that detects document type and applies appropriate extraction rules, outputting a flexible JSON schema that adapts to content.

Flexible JSON schemas that adapt to content create unpredictable output structures that complicate downstream processing and validation.

---

### Q16. You're designing a customer support agent that needs to handle complex mult…

**✅ Correct Answer: B**
> Deploy a multi-agent system with specialized agents for different support domains, a central orchestrator managing conversation state, and circuit breakers preventing infinite loops between agents.

The multi-agent approach with specialized agents, central orchestration, and circuit breakers provides proper separation of concerns while preventing common failure modes. This architecture scales better and maintains conversation coherence through proper state management.

**⚠️ Anti-Pattern: C**
> Create a monolithic agent that stores all conversation history in the context window and uses function calling to directly modify the customer database without validation layers.

Storing all conversation history in context leads to context saturation and creates security risks by allowing direct database modifications without proper validation layers.

---

### Q17. Your e-commerce platform needs Claude to process customer orders, update in…

**✅ Correct Answer: A**
> Create separate MCP servers for inventory management, order processing, and notifications, each with idempotent operations and proper error handling, connected through a unified tool registry.

Separate MCP servers with idempotent operations provide proper separation of concerns and fault isolation. The unified tool registry allows Claude to discover and use tools systematically while maintaining clean boundaries between different business domains.

**⚠️ Anti-Pattern: D**
> Design MCP tools that accept raw database queries as parameters, allowing Claude to directly construct SQL statements for maximum flexibility in data operations.

Accepting raw database queries as tool parameters creates a massive prompt injection surface and removes essential data validation safeguards.

---

### Q18. You're implementing Claude in a CI/CD pipeline to perform automated code re…

**✅ Correct Answer: B**
> Set up modular workflows with specific prompts for different code review aspects, implement proper context windowing for large files, and use circuit breakers to handle API failures gracefully.

Modular workflows with context windowing and circuit breakers provide scalable, reliable code review capabilities. This approach handles large codebases efficiently while maintaining review quality and system stability.

**⚠️ Anti-Pattern: D**
> Design workflows that inject raw git diff outputs without sanitization, use dynamic prompt construction from user input, and implement aggressive caching that persists across different repositories.

Dynamic prompt construction from user input creates a significant prompt injection surface that could compromise the code review process and expose sensitive information.

---

### Q19. You're building a data extraction system that processes financial documents…

**✅ Correct Answer: A**
> Use function calling with a comprehensive schema definition that includes all possible fields, and implement fallback logic to handle missing or malformed data gracefully.

Function calling provides the most reliable way to enforce structured output with schema validation, while the comprehensive schema with fallback logic handles the document format variability robustly. This approach leverages Claude's structured output capabilities rather than trying to enforce structure through post-processing or prompt engineering alone.

**⚠️ Anti-Pattern: C**
> Implement a single comprehensive prompt with detailed examples and use regex post-processing to enforce the JSON schema structure regardless of Claude's output format.

Using regex post-processing to enforce JSON structure creates a brittle system that may corrupt valid data and doesn't leverage Claude's native structured output capabilities.

---

### Q20. Your enterprise knowledge base system needs to handle conversations that ca…

**✅ Correct Answer: A**
> Implement sliding window context management that preserves the most recent exchanges while maintaining a summary of earlier conversation topics and key decisions.

Sliding window context management with summarization prevents context saturation while preserving essential conversation continuity. This approach maintains recent detailed context while compressing older information into summaries that remain accessible for reference.

**⚠️ Anti-Pattern: C**
> Use a fixed context window and simply truncate older messages when the limit is reached, ensuring the system always has the most recent context available.

Fixed truncation without preservation leads to context saturation and loss of important conversation history that may be needed for follow-up questions.

---

### Q21. You're designing a content moderation pipeline where multiple AI agents ana…

**✅ Correct Answer: A**
> Implement a coordinator agent that receives all individual agent assessments and makes final moderation decisions based on weighted confidence scores and business rules.

A coordinator agent provides centralized decision logic that can properly weight different violation types and apply business rules consistently. This architecture maintains agent independence while enabling sophisticated decision-making that considers the specific nature of different violations.

**⚠️ Anti-Pattern: D**
> Design agents to communicate directly with each other during processing, sharing intermediate findings to improve overall accuracy before reaching consensus.

Direct inter-agent communication creates a non-idempotent system where agent outputs depend on processing order and timing, making the pipeline unpredictable and difficult to debug.

---

### Q22. You're building a customer support agent that needs to access order history…

**✅ Correct Answer: A**
> Create separate tools for read operations (get_order_history) and write operations (update_ticket, send_notification) with explicit confirmation steps for state changes.

Separating read and write operations with confirmation steps follows the principle of least privilege and prevents accidental state changes. This design provides clear boundaries and makes the system more predictable and debuggable.

**⚠️ Anti-Pattern: B**
> Build a single unified tool called customer_service_handler that accepts a raw JSON object parameter containing all customer data and operation types to minimize tool switching overhead.

Using a raw object parameter creates an unbounded input surface that can lead to unpredictable behavior and makes tool validation nearly impossible.

---

### Q23. Your e-commerce platform needs Claude to generate product recommendations w…

**✅ Correct Answer: C**
> Design a structured prompt with clear constraints (budget range, category filters) and specify a JSON schema requiring product_id, price, and availability_status fields for each recommendation.

Structured prompts with explicit constraints and JSON schema output ensure deterministic, parseable results that can be directly integrated into e-commerce workflows. This approach minimizes ambiguity and enables reliable downstream processing.

**⚠️ Anti-Pattern: B**
> Include all product catalog data directly in the system prompt and ask Claude to select items, then manually validate the output against current inventory levels.

Including the entire product catalog in the system prompt leads to context bloat and makes the system unscalable as inventory grows.

---

### Q24. You're implementing Claude in a CI/CD pipeline to review pull requests for …

**✅ Correct Answer: B**
> Set up file-by-file processing with circuit breakers, timeout controls, and graceful degradation that skips non-critical files when the system is under heavy load.

Circuit breakers and timeout controls prevent cascade failures in CI/CD environments where reliability is critical. File-by-file processing with graceful degradation ensures the system remains responsive and doesn't block development workflows during peak usage.

**⚠️ Anti-Pattern: D**
> Configure automatic retry logic that continues attempting to process failed reviews indefinitely until successful, ensuring no security issues are missed.

Unbounded retry logic can cause resource exhaustion and block the entire CI/CD pipeline when Claude encounters persistent errors or rate limits.

---

### Q25. You're designing a multi-agent system where Claude agents handle document p…

**✅ Correct Answer: A**
> Implement a centralized orchestrator with explicit state transitions and structured message passing between agents, using JSON schemas for validation.

A centralized orchestrator with explicit state transitions provides clear control flow, proper error handling, and maintainable agent coordination while preventing context bloat and ensuring reliable inter-agent communication.

**⚠️ Anti-Pattern: D**
> Chain agents together with direct function calls, passing the full conversation history and accumulated context through each agent in the pipeline.

Passing full conversation history through each agent creates context bloat and can lead to context saturation as the pipeline processes more documents.
